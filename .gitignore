.DS_Store
*.sw[nop]
.bundle
.byebug_history
.env
db/*.sqlite3
log/*.log
rerun.txt
tags
!tags/
tmp/**/*
!tmp/cache/.keep
*.pyc

# Installation configuration (user-specific)
.install-config

# Local overrides (machine-specific settings, not version controlled)
*.local
.zshrc.local

# Cache directories
caches/